# Key

Key is a command-line utility for macOS, aiming to log human-interface devices (HID) events.

## Install

TBD.

## Usage

**Key** doesn't support subcommands for now. So, typing `key` is all you need to start logging the events.
