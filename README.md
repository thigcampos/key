# Key

Key is a command-line application for macOS, aiming to log `keydown` and `mousedown` events.

## ~Click~ Clique

Key is an utility used by **Clique** developers to map a few unknown events. It's specially 
useful to add support for unique devices. Unlike **Clique**, Key is available as an GPLv3 app.
